/** @type {import('tailwindcss').Config} */
module.exports = {
  // * the ** is to apply it to all the folders inside src. &     /*.{html,ts} => to target which types of files :
  content: ["./src/**/*.{html,ts}"],
  theme: {
    extend: {},
  },
  plugins: [],
};
